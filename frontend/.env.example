# Copia questo file in .env.development (locale) o .env.production
# e adatta i valori al tuo ambiente

# URL base dell'API PHP (relativo se frontend e backend sullo stesso server)
VITE_API_BASE_URL=/api

# Base path per Vite (usare ./ per XAMPP, / per server standard)
VITE_BASE_PATH=./
